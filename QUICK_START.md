# 🚀 Быстрый запуск для тестирования

## ✅ Что уже готово:

Все настроено для запуска **БЕЗ Google OAuth**! Вы можете тестировать Email/Password авторизацию прямо сейчас.

### Готовые настройки:
- ✅ `.env` файлы созданы (backend + frontend)
- ✅ Секретные ключи сгенерированы автоматически
- ✅ OpenRouter API ключ настроен (используется для примерки и AI ассистента)
- ✅ Mock режим для платежей включен
- ✅ Google OAuth отключен (можно тестировать Email/Password)

---

## 📝 Шаги для запуска:

### 1. Запустите PostgreSQL

```bash
# Запуск через Docker
docker-compose up -d postgres

# Или используйте локальный PostgreSQL (убедитесь, что он запущен на порту 5432)
```

### 2. Примените миграцию базы данных

```bash
cd backend
alembic upgrade head
```

### 3. Запустите Backend

```bash
# В той же директории backend
uvicorn app.main:app --reload
```

Backend будет доступен на: **http://localhost:8000**
Swagger API документация: **http://localhost:8000/docs**

### 4. Запустите Frontend (в новом терминале)

```bash
cd frontend
npm install  # если еще не устанавливали
npm run dev
```

Frontend будет доступен на: **http://localhost:5173**

---

## 🧪 Тестирование авторизации

### Email/Password регистрация

1. Откройте http://localhost:5173/register
2. Заполните форму:
   - Email: `test@example.com`
   - Password: `Test123!@#` (минимум 8 символов, заглавная, строчная, цифра, спецсимвол)
   - First Name: `Test`
   - Last Name: `User`
3. Нажмите "Create account"
4. Вы должны быть авторизованы и перенаправлены на главную

### Email/Password вход

1. Откройте http://localhost:5173/login
2. Введите email и пароль
3. Нажмите "Sign in"

### Проверка через Swagger UI

1. Откройте http://localhost:8000/docs
2. Попробуйте endpoint `POST /api/v1/auth/register`
3. Попробуйте endpoint `POST /api/v1/auth/login`
4. Скопируйте `access_token` из ответа
5. Нажмите кнопку "Authorize" в правом верхнем углу
6. Вставьте токен: `Bearer your-token-here`
7. Теперь можете тестировать защищенные endpoints

---

## 🔍 Что можно тестировать:

### ✅ Уже работает:
- Email/Password регистрация
- Email/Password вход
- JWT токены
- Password validation (сила пароля)
- Protected endpoints с авторизацией
- Генерация изображений (OpenRouter настроен)
- AI промпты (OpenRouter настроен)
- Freemium система (10 бесплатных действий в месяц)

### ⚠️ Требует настройки:
- Google Sign-In (нужны credentials из Google Cloud Console)
- Реальные платежи ЮKassa (включен mock режим)
- Telegram Bot (опционально, для обратной совместимости)

---

## 🎨 Доступные страницы:

- `/register` - Регистрация
- `/login` - Вход
- `/` - Главная (требует авторизации)

---

## ❓ Частые вопросы

### Q: Кнопка Google Sign-In не работает
**A:** Это нормально! Google OAuth credentials не настроены. Используйте Email/Password авторизацию.

### Q: Ошибка "Database connection failed"
**A:** Убедитесь, что PostgreSQL запущен:
```bash
docker-compose up -d postgres
# Или проверьте локальный PostgreSQL
```

### Q: Ошибка при миграции
**A:** Удалите базу данных и создайте заново:
```bash
docker-compose down -v
docker-compose up -d postgres
cd backend && alembic upgrade head
```

### Q: Хочу включить Google Sign-In
**A:** Следуйте инструкциям в [docs/GOOGLE_OAUTH_SETUP.md](docs/GOOGLE_OAUTH_SETUP.md), затем заполните в `backend/.env`:
```bash
GOOGLE_CLIENT_ID=ваш-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-ваш-secret
```

И в `frontend/.env`:
```bash
VITE_GOOGLE_CLIENT_ID=тот-же-client-id
```

---

## 🎉 Готово к тестированию!

Ваше приложение полностью работоспособно с Email/Password авторизацией!

**Следующие шаги:**
1. Протестируйте регистрацию и вход
2. Попробуйте создать изображение (через OpenRouter)
3. Проверьте freemium функциональность
4. При необходимости настройте Google OAuth позже

---

**Нужна помощь?** Смотрите:
- [ENV_SETUP_GUIDE.md](ENV_SETUP_GUIDE.md) - полное руководство по настройке
- [FRONTEND_COMPLETED_REPORT.md](FRONTEND_COMPLETED_REPORT.md) - детали frontend
- [docs/GOOGLE_OAUTH_SETUP.md](docs/GOOGLE_OAUTH_SETUP.md) - настройка Google OAuth
