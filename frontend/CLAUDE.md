# Frontend — AI Image Generator

## Назначение
AI Image Generator интерфейс для виртуальной примерки одежды/аксессуаров и редактирования изображений с помощью AI.

## Технологический стек
- **React 18**: Современный UI фреймворк
- **TypeScript**: Type-safe разработка
- **Vite**: Быстрый build tool
- **Tailwind CSS**: Utility-first CSS фреймворк
- **Telegram WebApp SDK**: Интеграция с Telegram
- **Zustand**: State management
- **React Router**: Маршрутизация
- **Framer Motion**: Анимации
- **Axios**: HTTP клиент
- **React Dropzone**: Загрузка файлов
- **React Step Wizard**: Квиз для примерки

## Структура проекта

```
frontend/
├── public/               # Статические файлы
├── src/
│   ├── api/              # API клиенты
│   │   ├── client.ts     # Axios instance
│   │   ├── auth.ts       # Авторизация
│   │   ├── fitting.ts    # Примерка
│   │   ├── editing.ts    # Редактирование
│   │   └── payments.ts   # Платежи
│   ├── components/       # React компоненты
│   │   ├── common/       # Общие компоненты (Button, Input, etc.)
│   │   ├── fitting/      # Компоненты примерки
│   │   ├── editing/      # Компоненты редактирования
│   │   └── layout/       # Layout компоненты (Header, Footer)
│   ├── pages/            # Страницы приложения
│   │   ├── Home.tsx      # Главная (выбор функции)
│   │   ├── Fitting.tsx   # Примерка одежды
│   │   ├── Editing.tsx   # Редактирование
│   │   ├── Profile.tsx   # Профиль и баланс
│   │   └── Payment.tsx   # Оплата
│   ├── hooks/            # Custom React hooks
│   │   ├── useTelegram.ts  # Telegram WebApp SDK
│   │   ├── useAuth.ts      # Авторизация
│   │   └── useWebSocket.ts # WebSocket для прогресса
│   ├── store/            # Zustand stores
│   │   ├── authStore.ts    # Состояние авторизации
│   │   ├── userStore.ts    # Данные пользователя
│   │   └── uiStore.ts      # UI состояние
│   ├── types/            # TypeScript types
│   │   ├── user.ts
│   │   ├── fitting.ts
│   │   └── editing.ts
│   ├── utils/            # Утилиты
│   │   ├── validation.ts   # Валидация файлов
│   │   └── formatters.ts   # Форматирование данных
│   ├── assets/           # Изображения, стили
│   │   └── styles/
│   │       └── index.css   # Глобальные стили + Tailwind
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── vite-env.d.ts     # Vite типы
├── index.html            # HTML шаблон
├── package.json          # Зависимости
├── tsconfig.json         # TypeScript конфигурация
├── vite.config.ts        # Vite конфигурация
├── tailwind.config.js    # Tailwind конфигурация
└── CLAUDE.md             # Этот файл
```

## КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА РАБОТЫ С КОДОМ

### ⚠️ ЗАПРЕЩЕНО без разрешения пользователя:

1. **НЕ УРЕЗАТЬ КОД**:
   - НИКОГДА не заменяйте существующий код на заглушки типа `{/* ... existing code ... */}` или `// ... rest of component ...`
   - НИКОГДА не пропускайте части кода с комментариями `{/* ... остальной JSX ... */}`
   - ВСЕГДА показывайте полный код компонента/функции при внесении изменений
   - При исправлении багов сохраняйте весь функционал, который уже работает

2. **НЕ СОЗДАВАТЬ ДУБЛИ КОМПОНЕНТОВ**:
   - ПЕРЕД созданием любого компонента, хука или утилиты — ОБЯЗАТЕЛЬНО проверьте, не существует ли уже такая реализация
   - Используйте Grep/Glob для поиска похожих компонентов (например, `Button`, `Modal`, `useWebSocket`)
   - Если компонент/хук уже есть — используйте его, не создавайте новый
   - Не плодите копии одной и той же логики в разных местах

3. **НЕ УДАЛЯТЬ ФУНКЦИОНАЛ**:
   - Функционал UI не должен урезаться без явного указания пользователя
   - Если нужно изменить логику компонента — спросите у пользователя, прежде чем удалять существующие возможности
   - Сохраняйте совместимость props компонентов

### ✅ ПРАВИЛЬНЫЙ ПОДХОД к исправлению багов в React:

```tsx
// ❌ НЕПРАВИЛЬНО:
const MyComponent: React.FC<Props> = ({ data }) => {
  {/* ... existing hooks ... */}

  const fixedValue = useMemo(() => calculateFixed(data), [data]); // Исправление

  return (
    <div>
      {/* ... existing JSX ... */}
    </div>
  );
};

// ✅ ПРАВИЛЬНО:
const MyComponent: React.FC<Props> = ({ data }) => {
  // Полный компонент с исправлением
  const [state, setState] = useState<State>(initialState);
  const { user } = useAuth();

  const fixedValue = useMemo(() => calculateFixed(data), [data]); // Исправленная строка

  useEffect(() => {
    fetchData();
  }, []);

  return (
    <div className="container">
      <Header title={state.title} />
      <Content data={fixedValue} />
      <Footer />
    </div>
  );
};
```

### 📋 ОБЯЗАТЕЛЬНЫЙ ЧЕК-ЛИСТ перед написанием кода:

1. [ ] Проверил, существует ли уже такой компонент/хук/утилита? (Grep/Glob)
2. [ ] Уверен, что не дублирую существующий код?
3. [ ] Показываю полный код компонента без заглушек?
4. [ ] Сохраняю весь существующий функционал UI?
5. [ ] Спросил у пользователя, если собираюсь удалить функционал?

## Ключевые модули

### 1. Авторизация (hooks/useTelegram.ts)
- Получение Telegram initData
- Валидация через HMAC SHA-256 (на backend)
- Получение JWT-токена
- Сохранение в localStorage

### 2. Примерка одежды (pages/Fitting.tsx)
- **Компоненты:**
  - `FittingWizard` — квиз с react-step-wizard
  - `PhotoUploadStep` — загрузка фото пользователя
  - `ItemUploadStep` — загрузка одежды/аксессуара
  - `ZoneSelectionStep` — выбор зоны для аксессуара
  - `GenerationProgress` — прогресс генерации (WebSocket)
  - `ResultPreview` — результат с зумом и скачиванием

- **Логика:**
  - Валидация файлов (JPEG/PNG, ≤5MB)
  - Draft-система для сохранения прогресса
  - WebSocket для real-time прогресса
  - Списание 2 кредитов

### 3. Редактирование (pages/Editing.tsx)
- **Компоненты:**
  - `ChatInterface` — чат с AI-ассистентом
  - `MessageList` — история сообщений + изображения
  - `PromptInput` — ввод запроса
  - `PromptSuggestions` — 3 варианта промптов от AI
  - `ImagePreview` — превью с зумом
  - `ResetChatButton` — сброс истории

- **Логика:**
  - Загрузка фото для редактирования
  - Отправка запроса → получение 3 промптов
  - Выбор/редактирование промпта → генерация
  - История в одном окне (скролл)
  - WebSocket для прогресса
  - Списание: 1 кредит за запрос + 1 за генерацию

### 4. Монетизация (pages/Payment.tsx)
- **Компоненты:**
  - `BalanceCard` — отображение баланса и подписки
  - `TariffSelector` — выбор подписки/кредитов
  - `PaymentWizard` — квиз оплаты (3 шага)
  - `TransactionHistory` — история платежей

- **Логика:**
  - Проверка баланса перед действием
  - Redirect на ЮKassa
  - Обработка возврата после оплаты
  - Уведомления о низком балансе

### 5. WebSocket для прогресса (hooks/useWebSocket.ts)
```typescript
const { status, progress, error } = useWebSocket(taskId);

// status: 'connecting' | 'connected' | 'processing' | 'completed' | 'failed'
// progress: 0-100
```

### 6. Telegram WebApp SDK (hooks/useTelegram.ts)
```typescript
const {
  user,
  initData,
  MainButton,
  BackButton,
  theme,
  expand,
  close,
} = useTelegram();
```

## Основные страницы

### Home (/)
- Выбор функции: "Примерка" или "Редактирование"
- Отображение баланса кредитов
- Реферальная ссылка

### Fitting (/fitting)
- Квиз: фото пользователя → фото одежды → зона (для аксессуаров) → генерация
- Прогресс генерации
- Результат с зумом/скачиванием

### Editing (/editing)
- Загрузка фото
- Чат с AI (история сообщений + изображения)
- Генерация по промпту
- Кнопка сброса чата

### Profile (/profile)
- Баланс кредитов
- Активная подписка
- История действий
- Реферальная программа

### Payment (/payment)
- Тарифы (подписки + кредиты)
- Квиз оплаты
- История транзакций

## State Management (Zustand)

### authStore
```typescript
{
  isAuthenticated: boolean
  token: string | null
  user: User | null
  login: (initData: string) => Promise<void>
  logout: () => void
}
```

### userStore
```typescript
{
  balance: number
  subscription: Subscription | null
  actions: Action[]
  updateBalance: (credits: number) => void
}
```

### uiStore
```typescript
{
  isLoading: boolean
  modal: ModalState | null
  toast: ToastState | null
}
```

## Валидация файлов (utils/validation.ts)

```typescript
// Валидация на клиенте
const validateFile = (file: File): ValidationResult => {
  // 1. Проверка типа MIME
  if (!['image/jpeg', 'image/png'].includes(file.type)) {
    return { valid: false, error: 'Только JPEG/PNG' }
  }

  // 2. Проверка размера
  if (file.size > 5 * 1024 * 1024) {
    return { valid: false, error: 'Максимум 5MB' }
  }

  return { valid: true }
}
```

## Обработка ошибок

- **Network errors**: Retry через axios interceptor
- **API errors**: Toast уведомления
- **Validation errors**: Inline errors в формах
- **WebSocket errors**: Reconnect автоматически

## Темизация Telegram

Приложение автоматически адаптируется под тему Telegram:

```css
/* Tailwind config использует CSS переменные от Telegram */
bg-telegram-bg
text-telegram-text
bg-telegram-button
```

## Запуск

```bash
# Установка зависимостей
npm install

# Копирование .env
cp .env.example .env
# Заполнить VITE_API_BASE_URL

# Запуск dev сервера
npm run dev

# Build для production
npm run build

# Preview production build
npm run preview

# Линтинг
npm run lint

# Тесты
npm run test
npm run test:watch
```

## TODO
- [ ] Создать базовые компоненты (Button, Input, Modal)
- [ ] Реализовать авторизацию через Telegram
- [ ] Создать квиз для примерки
- [ ] Создать чат-интерфейс для редактирования
- [ ] Интегрировать WebSocket для прогресса
- [ ] Реализовать монетизацию
- [ ] Добавить анимации (Framer Motion)
- [ ] Написать тесты
