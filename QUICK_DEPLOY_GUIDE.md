# 🚀 Быстрое развертывание AI Image Bot на VPS

## 📋 Информация о сервере

- **IP:** 185.135.82.109
- **Пользователь:** root
- **Пароль:** huIRNA0
- **Домен:** ai-bot-media.mix4.ru
- **Portainer:** http://185.135.82.109:9000/

---

## Шаг 1: Настройка DNS на Beget ⏱️ 5 минут

### 1.1 Зайдите в панель Beget
1. Откройте https://cp.beget.com
2. Войдите в систему

### 1.2 Добавьте A-запись
1. Найдите раздел **"DNS"** или **"Управление DNS"**
2. Выберите домен **mix4.ru**
3. Нажмите **"Добавить запись"**
4. Заполните:
   ```
   Тип:     A
   Имя:     ai-bot-media
   Значение: 185.135.82.109
   TTL:     3600
   ```
5. Нажмите **"Сохранить"**

### 1.3 Проверьте DNS (через 15-30 минут)
```bash
# На вашем компьютере выполните:
ping ai-bot-media.mix4.ru

# Должно показать: 185.135.82.109
```

---

## Шаг 2: Подключитесь к VPS ⏱️ 1 минута

### Через Terminal (macOS/Linux):
```bash
ssh root@185.135.82.109
# Пароль: huIRNA0
```

### Через PuTTY (Windows):
1. Host: `185.135.82.109`
2. Port: `22`
3. Connection type: `SSH`
4. Login as: `root`
5. Password: `huIRNA0`

---

## Шаг 3: Скачайте и запустите скрипт развертывания ⏱️ 5-10 минут

### 3.1 Скачайте скрипт
```bash
cd /root
curl -O https://raw.githubusercontent.com/cherus80/ai-image-bot/master/vps-deploy-script.sh
chmod +x vps-deploy-script.sh
```

### 3.2 Отредактируйте конфигурацию в скрипте
```bash
nano vps-deploy-script.sh
```

Найдите секцию **"КОНФИГУРАЦИЯ"** и заполните:
```bash
# Telegram Bot (получить у @BotFather)
TELEGRAM_BOT_TOKEN="6281234567:AAHdqTcvbXyz1234567890abcdefGHIJKL"
BOT_USERNAME="your_bot_name"

# API Keys
KIE_AI_API_KEY="ваш_ключ_от_kie_ai"
OPENROUTER_API_KEY="ваш_ключ_от_openrouter"
```

**Сохраните:** `Ctrl+X` → `Y` → `Enter`

### 3.3 Запустите скрипт
```bash
./vps-deploy-script.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит необходимые пакеты
- ✅ Настроит firewall
- ✅ Склонирует репозиторий
- ✅ Создаст .env файл с секретными ключами
- ✅ Настроит Nginx

---

## Шаг 4: Запустите приложение через Docker Compose ⏱️ 5-7 минут

```bash
cd /opt/ai-image-bot

# Запустить все контейнеры
docker-compose -f docker-compose.prod.yml up -d

# Дождитесь завершения (может занять 5-7 минут при первом запуске)
# Docker скачает и соберет все образы

# Проверьте статус
docker-compose -f docker-compose.prod.yml ps
```

**Все контейнеры должны быть в статусе "Up":**
- ✅ ai_image_bot_postgres_prod
- ✅ ai_image_bot_redis_prod
- ✅ ai_image_bot_backend_prod
- ✅ ai_image_bot_frontend_prod
- ✅ ai_image_bot_celery_worker_prod
- ✅ ai_image_bot_celery_beat_prod
- ✅ ai_image_bot_telegram_prod

### Если что-то не запустилось:
```bash
# Посмотрите логи проблемного контейнера
docker logs ai_image_bot_backend_prod

# Перезапустите конкретный контейнер
docker restart ai_image_bot_backend_prod
```

---

## Шаг 5: Инициализация базы данных ⏱️ 1 минута

```bash
# Запустите миграции
docker exec -it ai_image_bot_backend_prod alembic upgrade head

# Проверьте создание таблиц
docker exec -it ai_image_bot_postgres_prod psql -U postgres -d ai_image_bot -c "\dt"
```

Должны увидеть таблицы:
- users
- generations
- chat_messages
- payments
- referrals
- alembic_version

---

## Шаг 6: Получите SSL сертификат ⏱️ 2 минуты

**⚠️ ВАЖНО: Выполняйте ТОЛЬКО после того, как DNS заработает!**

Проверьте DNS:
```bash
ping ai-bot-media.mix4.ru
# Должен вернуть: 185.135.82.109
```

Если DNS работает, получите сертификат:
```bash
# Остановите nginx временно
systemctl stop nginx

# Получите сертификат
certbot certonly --standalone -d ai-bot-media.mix4.ru

# Следуйте инструкциям:
# - Введите email для уведомлений
# - Согласитесь с Terms of Service
# - Выберите, хотите ли делиться email

# Запустите nginx снова
systemctl start nginx

# Или автоматически через nginx:
certbot --nginx -d ai-bot-media.mix4.ru
```

---

## Шаг 7: Настройте Telegram бота ⏱️ 2 минуты

### 7.1 Откройте Telegram и найдите @BotFather

### 7.2 Настройте Menu Button
```
/mybots
→ Выберите вашего бота
→ Bot Settings
→ Menu Button
→ Edit menu button URL
→ https://ai-bot-media.mix4.ru
```

### 7.3 Настройте команды (опционально)
```
/mybots
→ Выберите вашего бота
→ Edit Bot
→ Edit Commands

Введите:
start - Запустить бота
fitting - Примерка одежды
editing - Редактирование изображений
profile - Мой профиль
help - Помощь
```

---

## Шаг 8: Проверка работы ✅

### 8.1 Проверьте Backend API
```bash
curl https://ai-bot-media.mix4.ru/api/v1/health
# Должно вернуть: {"status":"ok"}
```

### 8.2 Откройте Frontend
Перейдите в браузере: https://ai-bot-media.mix4.ru

### 8.3 Проверьте бота в Telegram
1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Должна открыться Web App

### 8.4 Проверьте все контейнеры
```bash
docker ps
# Все контейнеры должны быть "Up"
```

---

## 🎉 Готово!

Ваш AI Image Bot успешно развернут!

### 📊 Полезные команды:

```bash
# Просмотр логов всех контейнеров
docker-compose -f /opt/ai-image-bot/docker-compose.prod.yml logs -f

# Просмотр логов backend
docker logs -f ai_image_bot_backend_prod

# Просмотр логов celery worker
docker logs -f ai_image_bot_celery_worker_prod

# Перезапуск всех контейнеров
docker-compose -f /opt/ai-image-bot/docker-compose.prod.yml restart

# Остановка всех контейнеров
docker-compose -f /opt/ai-image-bot/docker-compose.prod.yml down

# Запуск всех контейнеров
docker-compose -f /opt/ai-image-bot/docker-compose.prod.yml up -d

# Использование ресурсов
docker stats

# Backup базы данных
docker exec -it ai_image_bot_postgres_prod pg_dump -U postgres ai_image_bot > /opt/backups/backup_$(date +%Y%m%d).sql
```

---

## 🔧 Альтернатива: Развертывание через Portainer UI

Если предпочитаете использовать веб-интерфейс:

1. Откройте http://185.135.82.109:9000/
2. Войдите (admin / Rusik_773arm258)
3. Выберите **Stacks** → **Add stack**
4. Название: `ai-image-bot`
5. Выберите **Web editor**
6. Скопируйте содержимое файла `/opt/ai-image-bot/docker-compose.prod.yml`
7. В разделе **Environment variables** добавьте переменные из `/opt/ai-image-bot/.env`
8. Нажмите **Deploy the stack**

---

## 📞 Нужна помощь?

Если возникли проблемы:

1. **Проверьте логи:**
   ```bash
   docker logs ai_image_bot_backend_prod
   ```

2. **Проверьте статус контейнеров:**
   ```bash
   docker ps -a
   ```

3. **Проверьте nginx:**
   ```bash
   systemctl status nginx
   tail -f /var/log/nginx/ai-image-bot-error.log
   ```

4. **Перезапустите всё:**
   ```bash
   cd /opt/ai-image-bot
   docker-compose -f docker-compose.prod.yml restart
   systemctl restart nginx
   ```

---

## ⚠️ Важные замечания

1. **API ключи:** Обязательно заполните в `.env` файле:
   - `TELEGRAM_BOT_TOKEN`
   - `KIE_AI_API_KEY`
   - `OPENROUTER_API_KEY`

2. **Платежи:** По умолчанию включен mock режим (`PAYMENT_MOCK_MODE=true`)
   - Для реальных платежей укажите ЮKassa credentials и установите `PAYMENT_MOCK_MODE=false`

3. **Backup:** Настройте автоматический backup базы данных (см. PORTAINER_DEPLOY.md)

4. **Мониторинг:** Регулярно проверяйте логи и использование ресурсов

5. **Обновления:** Для обновления приложения:
   ```bash
   cd /opt/ai-image-bot
   git pull
   docker-compose -f docker-compose.prod.yml build
   docker-compose -f docker-compose.prod.yml up -d
   ```
