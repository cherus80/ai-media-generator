Общие положения
1.1. Наименование проекта
AI Image Generator Bot — Telegram Web App для виртуальной примерки одежды/аксессуаров и редактирования изображений с помощью модели Nano Banana от kie.ai. Проект включает Telegram-бот для авторизации и Telegram Web App как основной интерфейс с двумя основными функциями: примеркой и редактированием.

1.2. Цель разработки
Создать удобное приложение для пользователей без опыта в AI, с двумя функциями: (1) примерка одежды/аксессуаров на фото (с фиксированным промптом и квизом без AI-ассистента); (2) редактирование изображений (с AI-ассистентом, чат-историей как в Google AI Studio). Монетизация через гибридную модель: Freemium (10 бесплатных действий/месяц, где действие = генерация или запрос), подписки (299₽/50 действий, 499₽/150 действий, 899₽/500 действий) и покупка кредитов (199₽/100 кредитов). Приложение масштабируемое, безопасное, с учётом расходов (API kie.ai ~2.5₽/генерацию, OpenRouter ~0.07₽/запрос).

1.3. Задачи проекта
Реализовать интерфейс в Telegram Web App с выбором функции (примерка или редактирование), квизом для примерки и чат-подобным интерфейсом для редактирования.

Интегрировать backend для обработки фото, хранения истории (чат и изображения), монетизации и учёта кредитов.

Обеспечить безопасность (авторизация по Telegram ID, защита API, GDPR для фото с авто-удалением через 24ч).

Автоматизировать платежи через ЮKassa с учётом комиссии 2.8% и НПД 4%.

Поддерживать логирование, мониторинг и резервное копирование.

1.4. Заказчик и исполнитель
Заказчик: Пользователь (разработчик на основе Claude Code).

Исполнитель: Claude Code для генерации кода с последующей доработкой.

Документы: API kie.ai, OpenRouter, ЮKassa; Telegram Bot API; шаблоны React/FastAPI.

Описание системы
2.1. Назначение и область применения
Приложение — Telegram Web App для двух функций: (1) примерки одежды/аксессуаров на фото пользователя/модели; (2) редактирования изображений (добавление/удаление/изменение элементов). Целевая аудитория: Новички 18-45 лет, интересующиеся модой и фото. Область: Мобильное/десктопное приложение в Telegram. Интерфейс: Пошаговый квиз с подсказками для примерки; чат-окно с историей для редактирования (как в Google AI Studio, с кнопкой сброса). Язык: Русский.

2.2. Целевая аудитория
Новички: Квиз и чат упрощают UX (e.g., фиксированный промпт для примерки; AI для редактирования).

Платящие пользователи: Подписки/кредиты для неограниченного доступа.

Администратор: Мониторинг статистики (доход минус расходы/налоги).

2.3. Общая архитектура
Frontend: React.js + Telegram WebApp SDK + react-step-wizard для квиза (примерка); чат-компонент (как в AI Studio) для редактирования. Хостинг: Beget shared или VPS (Docker/Portainer).

Backend: FastAPI (Python 3.11) + SQLAlchemy/PostgreSQL. Хостинг: Beget VPS (Docker/Portainer).

База данных: PostgreSQL для профилей, истории чата (редактирование), генераций, платежей, draft квиза.

Интеграции: kie.ai (Nano Banana для генерации/редактирования), OpenRouter (Claude Haiku только для редактирования), ЮKassa (платежи), Telegram Bot API.

Коммуникация: HTTPS REST + WebSocket для прогресса генерации и чата.

2.4. User Flow (основной сценарий)
/start в боте → Открытие Web App → Авторизация по Telegram ID → Выбор функции: "Примерка" или "Редактирование".

Функция 1 (Примерка): Квиз (Шаг 1: Загрузка фото пользователя/модели; Шаг 2: Загрузка одежды/аксессуара; Опциональный шаг 3: Выбор зоны для аксессуара; Кнопка "Примерить" → фиксированный промпт в kie.ai).

Функция 2 (Редактирование): Загрузка фото → Чат с AI (запросы промптов → генерация) → История сообщений/изображений → Кнопка "Сброс чата".

Монетизация: Списание 1-2 кредитов за действие (генерация в примерке; запрос+генерация в редактировании). При низком балансе — квиз оплаты.

Функциональные требования
3.1. Модули системы
Модуль 1: Авторизация и профиль
Функции: Регистрация по Telegram ID, баланс (кредиты, подписка), история действий (примерки/редактирования).

User Flow: Авто при запуске. Подсказка: "Выберите функцию: примерка или редактирование. Баланс: 5 кредитов".

Результат: JWT-сессия. Ошибка: "Авторизуйтесь заново".

Интеграции: Telegram API, PostgreSQL.

Модуль 2: Функция 1 — Примерка одежды/аксессуаров
Функции: Квиз для загрузки двух фото (пользователь/модель + одежда/аксессуар), фиксированный промпт (e.g., "Примерить [описание одежды] на [описание фото пользователя] в реалистичном стиле"). Без AI-ассистента. Для аксессуаров: Добавить шаг выбора зоны (кнопки: "На голову (очки/шляпа)", "На шею (украшения)", "На руки (часы/браслеты)", "На ноги (обувь)"). Зона включается в промпт (e.g., "Примерить очки на голову модели").

User Flow: Шаг 1 — "📸 Загрузите своё фото или модели (JPEG/PNG, <5MB, полный рост)". Шаг 2 — "👗 Загрузите фото одежды/аксессуара (<5MB)". Шаг 3 (если аксессуар: авто-определение по метаданным или пользовательский выбор) — "Выберите зону: голова/шея/руки/ноги". Кнопка "Примерить" → Прогресс → Результат. Списание 2 кредитов (за два фото).

Результат: URL изображения с примеркой. Freemium: 10/месяц с водяным знаком. Ошибка: "Загрузите оба фото".

Интеграции: kie.ai (/generate, model: nano-banana, images: [{user_photo}, {item_photo}], prompt: фиксированный с зоной ~2.5₽/генерацию).

Модуль 3: Функция 2 — Редактирование изображений
Функции: Загрузка фото → Чат с AI-ассистентом (составление промпта для добавления/удаления/изменения, e.g., "Добавь красное платье"). Сохранение истории чата (сообщения + изображения) в одном окне (скролл, как в Google AI Studio). Кнопка "Сброс чата" (очистка истории для новой загрузки фото). AI генерирует 3 варианта промптов.

User Flow: Выбор функции → Загрузка фото → Чат: "💡 Опишите изменения (e.g., 'Удалить фон')" → AI-ответ (промпты) → Выбор/редактирование → "Генерировать" → Результат в чате. История: Список сообщений/изображений (зум/скачивание). Сброс: "Начать новый чат?" (с подтверждением). Списание: 1 кредит за запрос к ассистенту + 1 за генерацию (~0.07₽ + 2.5₽).

Результат: Обновлённые изображения в истории. Лимит: По подписке; история хранится до 30 дней.

Интеграции: OpenRouter (модель: claude-3-haiku-20240307 для ассистента); kie.ai для генерации.

Модуль 4: Монетизация и учёт токенов
Функции: Гибрид: Freemium (10 действий/месяц), подписки, кредиты. Учёт: 1 действие = 1 генерация (примерка) или (запрос ассистента + генерация) (редактирование). В примере: 2 кредита/действие. В редактировании: Токены OpenRouter логируются (input/output), списание по использованию (e.g., >500 токенов = 1 кредит). Квиз оплаты (3 шага: выбор → сумма → подтверждение).

User Flow: Проверка баланса перед действием. В редактировании: Списание за каждый обмен (ассистент) + генерацию; история не тратит кредиты. При низком: "Купите кредиты и продолжите чат!".

Результат: Начисление после webhook. Учёт НПД 4% (логи в CSV для "Мой налог"). Интеграции: ЮKassa (/payments, webhook, комиссия 2.8%).

Модуль 5: Административная панель
Функции: Статистика (действия, доход минус расходы/налоги/токены), управление тарифами, просмотр историй чатов.

User Flow: /admin (секретный ключ). Подсказка: "Экспорт отчёта с токенами".

Результат: Графики (Chart.js), CSV с налогами/токенами.

Интеграции: PostgreSQL.

Модуль 6: Реферальная программа
Функции: +10 кредитов за друга после его первого действия.

User Flow: В профиле/чате. Подсказка: "Пригласите за бонус!".

Результат: Генерация ссылки, начисление.

Интеграции: Telegram.shareUrl.

3.2. Обработка ошибок
API: "Попробуйте позже" + лог в Sentry.

Баланс: Квиз на покупку.

Фото/чат: Валидация (размер/NSFW); сброс не удаляет платные истории.

Технические требования
4.1. Платформа и технологии
Frontend: React 18+ (Vite, TypeScript, Tailwind, Framer Motion). Библиотеки: react-dropzone (фото), react-chat-ui (для редактирования), react-step-wizard (квиз).

Backend: FastAPI 0.100+, Python 3.11, SQLAlchemy, Celery (async).

БД: PostgreSQL 15 (JSONB для истории чата/ draft).

API: REST JSON, CORS, rate limiting (10/min/user).

Язык: Русский (i18n).

4.2. Интеграции
kie.ai: POST /generate {model: "nano-banana", images: [user_photo, item_photo], prompt: фиксированный с зоной (примерка) или от AI (редактирование)}. Async.

OpenRouter: Только для редактирования — POST /chat/completions {model: "claude-3-haiku", messages: с историей чата для контекста}.

ЮKassa: POST /payments, webhook. Токены: Лог в БД (Payments: tokens_used decimal).

Telegram: Bot Token, WebApp SDK (initData HMAC).

Другое: Sentry (логи), Docker (деплой).

4.3. База данных
Users: id (PK), telegram_id (unique), balance_credits (int), subscription_type (enum), subscription_end (date).

Generations: id (PK), user_id (FK), type (enum: 'fitting', 'editing'), user_photo_url, item_photo_url (для примерки), prompt, image_url, credits_spent (int), tokens_used (decimal для редактирования).

ChatHistory: id (PK), user_id (FK), session_id (unique), messages (JSONB: [{role, content, image_url}]), created_at.

Payments: id (PK), user_id (FK), amount (decimal), status (enum), yookassa_id, tax_amount (decimal), tokens_used (decimal).

Referrals: id (PK), referrer_id (FK), referred_id (FK), credits_awarded (int).

4.4. Производительность
Отклик: <2 сек (кэш промптов/историй).

Нагрузка: 1000 пользователей (scale VPS).

Адаптация: Responsive для Telegram.

Нефункциональные требования
5.1. Безопасность
Auth: Telegram initData + JWT.

Защита: HTTPS (Let's Encrypt), ORM (SQL-injection), sanitization (XSS), временное хранение фото/чатов (24ч/30 дней, GDPR).

Налоги: Лог платежей/токенов для "Мой налог" (4% НПД).

5.2. Надежность
Логи: Sentry + ELK (опционально).

Backup: Ежедневный дамп БД (включая чаты).

Обновления: GitHub Actions.

5.3. Тестирование
Unit: Pytest (backend), Jest (frontend).

E2E: Playwright (квиз примерки; чат редактирования с историей/сбросом).

Edge: Выбор зоны для аксессуаров; учёт токенов в чате (mock OpenRouter).

Этапы реализации в Claude Code
Backend базовый (промпт: "Создай FastAPI по ТЗ с модулями для примерки и редактирования").

Интеграции API (kie.ai фиксированный промпт; OpenRouter с историей чата).

Frontend (квиз для примерки; чат-UI для редактирования).

Деплой (Beget VPS Docker) и тесты (учёт кредитов/токенов).

Итерации: "Исправь ошибку по ТЗ (e.g., зона аксессуара)".