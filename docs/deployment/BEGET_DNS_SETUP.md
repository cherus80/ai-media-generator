# 🌐 Настройка поддомена на Beget для VPS

## Задача
Создать поддомен `ai-bot-media.mix4.ru` и привязать его к VPS на dockerhosting.ru (IP: 185.135.82.109)

---

## 📋 Пошаговая инструкция

### Способ 1: Через панель Beget (Рекомендуется)

#### Шаг 1: Войдите в панель управления Beget

1. Перейдите на https://cp.beget.com
2. Введите логин и пароль
3. Войдите в систему

#### Шаг 2: Перейдите в раздел DNS

1. В левом меню найдите раздел **"Домены и поддомены"** или **"DNS"**
2. Выберите домен **mix4.ru** из списка
3. Нажмите на кнопку **"Управление DNS"** или **"DNS-записи"**

#### Шаг 3: Добавьте A-запись для поддомена

1. Нажмите **"Добавить запись"** или **"Создать новую запись"**
2. Заполните поля:

```
Тип записи: A
Имя (поддомен): ai-bot-media
Значение (IP адрес): 185.135.82.109
TTL: 3600 (или оставьте по умолчанию)
```

3. Нажмите **"Сохранить"** или **"Добавить"**

#### Шаг 4: Проверка DNS записи

После добавления записи, вы должны увидеть в списке:

```
ai-bot-media.mix4.ru  →  A  →  185.135.82.109
```

---

### Способ 2: Через файл zone (для опытных пользователей)

Если у вас есть доступ к редактированию zone файла:

1. Перейдите в раздел **"DNS"** → **"Редактор zone файла"**
2. Найдите секцию с A-записями для домена mix4.ru
3. Добавьте строку:

```
ai-bot-media    IN    A    185.135.82.109
```

4. Сохраните изменения

---

### Способ 3: Через API Beget (автоматизация)

Если хотите использовать API:

```bash
curl -X POST "https://api.beget.com/api/dns/changeRecords" \
  -H "Content-Type: application/json" \
  -d '{
    "login": "ваш_логин",
    "passwd": "ваш_пароль",
    "input_format": "json",
    "output_format": "json",
    "input_data": {
      "fqdn": "mix4.ru",
      "records": {
        "A": [
          {
            "subdomain": "ai-bot-media",
            "value": "185.135.82.109"
          }
        ]
      }
    }
  }'
```

---

## ⏱️ Время распространения DNS

- **Обычно:** 5-30 минут
- **Максимум:** до 24-48 часов (в редких случаях)
- **Beget обычно быстро:** 5-15 минут

---

## ✅ Проверка настройки DNS

### Через веб-сервисы:

1. **DNS Checker:** https://dnschecker.org
   - Введите: `ai-bot-media.mix4.ru`
   - Проверьте, что IP везде показывается как `185.135.82.109`

2. **WhatsMyDNS:** https://whatsmydns.net
   - Введите: `ai-bot-media.mix4.ru`
   - Тип: `A`
   - Проверьте распространение по миру

### Через командную строку:

#### На Windows:

```cmd
nslookup ai-bot-media.mix4.ru
```

Должно вернуть:
```
Name:    ai-bot-media.mix4.ru
Address: 185.135.82.109
```

#### На macOS/Linux:

```bash
# Вариант 1: dig
dig ai-bot-media.mix4.ru

# Вариант 2: host
host ai-bot-media.mix4.ru

# Вариант 3: nslookup
nslookup ai-bot-media.mix4.ru
```

Ожидаемый результат:
```
ai-bot-media.mix4.ru has address 185.135.82.109
```

### Проверка доступности через ping:

```bash
ping ai-bot-media.mix4.ru
```

Должно показать:
```
PING ai-bot-media.mix4.ru (185.135.82.109): 56 data bytes
64 bytes from 185.135.82.109: icmp_seq=0 ttl=54 time=50.2 ms
```

---

## 🔧 Возможные проблемы и решения

### Проблема 1: DNS не обновляется

**Решение:**
- Подождите 15-30 минут
- Очистите DNS кеш на вашем компьютере:
  - **Windows:** `ipconfig /flushdns`
  - **macOS:** `sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder`
  - **Linux:** `sudo systemd-resolve --flush-caches`

### Проблема 2: "Запись уже существует"

**Решение:**
- Проверьте, нет ли уже записи для `ai-bot-media`
- Если есть - отредактируйте существующую запись, изменив IP на 185.135.82.109

### Проблема 3: Не находите раздел DNS

**Решение:**
- В панели Beget он может называться по-разному:
  - "DNS-записи"
  - "Управление DNS"
  - "Домены и поддомены" → выбрать домен → "DNS"
- Если не можете найти - обратитесь в поддержку Beget

### Проблема 4: Доступа к DNS записям нет

**Решение:**
- Убедитесь, что домен `mix4.ru` делегирован на DNS Beget
- Проверьте, что вы вошли под правильным аккаунтом
- Обратитесь в поддержку Beget

---

## 📞 Контакты поддержки Beget

- **Сайт:** https://beget.com/ru/support
- **Email:** support@beget.com
- **Telegram:** @beget_support
- **Телефон:** +7 (495) 721-80-88

---

## 🎯 Следующие шаги

После успешной настройки DNS:

1. ✅ Подождите 15-30 минут для распространения DNS
2. ✅ Проверьте доступность поддомена через `ping ai-bot-media.mix4.ru`
3. ✅ Переходите к развертыванию приложения на VPS
4. ✅ Настройте SSL сертификат Let's Encrypt
5. ✅ Протестируйте работу приложения

---

## 📝 Итоговая конфигурация DNS

После настройки у вас будет:

```
Домен:           ai-bot-media.mix4.ru
Тип записи:      A
IP адрес:        185.135.82.109
Назначение:      AI Image Generator Bot
SSL:             Будет настроен через Let's Encrypt
```

---

## ✅ Чек-лист

- [ ] Вошел в панель управления Beget
- [ ] Нашел раздел DNS для домена mix4.ru
- [ ] Добавил A-запись для ai-bot-media → 185.135.82.109
- [ ] Сохранил изменения
- [ ] Подождал 15-30 минут
- [ ] Проверил через nslookup/dig
- [ ] Проверил через ping
- [ ] DNS работает корректно - можно переходить к развертыванию!
