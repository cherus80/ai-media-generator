# 📊 Project Status - AI Generator

**Последнее обновление**: 2026-01-25
**Текущая версия**: v0.15.24
**Статус**: ✅ Production Ready (Auth Fixed + Tested with Playwright)

---

## ⚠️ Обновление интеграции kie.ai (2025-11-25)
- Клиент переписан под новый API: `POST /api/v1/jobs/createTask` + `GET /api/v1/gpt4o-image/record-info`.
- Передаются **URL** изображений (fields: `filesUrl` для edit, `user_photo` + `item_photo` для try-on). Base64 не используется.
- Требуется публичный доступ к вашим файлам `/uploads/*` (или туннель). Если `BACKEND_URL` локальный (`http://localhost:8000`), kie.ai не сможет скачать изображения и задачи зависнут по таймауту.
- Для отладки есть флаг `KIE_AI_DISABLE_FALLBACK=true`, чтобы видеть ошибки kie.ai без перехода на OpenRouter.

## 🚀 Что реализовано
### Обновление 2026-01-25 (v0.15.24)
- Админ-панель: таб «Оповещения» с обязательным заголовком и рассылкой выбранным пользователям или всем (включая неактивных/заблокированных).
- Пользовательский интерфейс: страница «Оповещения» со списком прочитанных/непрочитанных сообщений и автопометкой прочтения при открытии.
- Админ-примеры: выбор меток из выпадающего списка с уже созданными тегами + отдельное поле для новых меток.
- В шапке приложения: колокольчик с числом непрочитанных и одноразовая подсказка при наличии новых сообщений.
- Backend: таблица `notifications` и API `/api/v1/notifications` + `/api/v1/admin/notifications`.

### Обновление 2026-01-12 (v0.15.23)
- Celery beat пишет файл расписания в /app/logs, чтобы корректно работать от non-root пользователя.
- Для Celery worker/beat задан HOME=/home/appuser, чтобы не было попыток чтения /root/.postgresql/postgresql.key.

### Обновление 2026-01-11 (v0.15.23)
- В редакторе фото и генерации по образцу показано предупреждение о превышении 2000 символов и блокируется запуск до сокращения промпта.
- Усилен контур безопасности: обязательная подпись webhook ЮKassa в продакшене, проверка URL вложений как локальных /uploads, лимит разрешения изображений, rate limit по X-Real-IP.
- В чате редактирования результат не дублирует исходный промпт; Celery переведён на запуск от non-root пользователя.

### Обновление 2026-01-04 (v0.15.23)
- Примеры генераций получили метки, фильтрацию и сортировку по востребованности.
- На странице выбора функций выводится блок топ-6 самых популярных примеров с CTA “Смотреть больше примеров”.
- Видео-инструкции поддерживают краткое описание в админке и на публичной странице.
- Добавлены robots.txt и sitemap.xml, обновлены SEO-мета на публичных страницах.
- Тесты синхронизированы с billing v5/async-валидатором файлов; интеграционные тесты пропускаются без доступной PostgreSQL.
- Добавлен .bandit.yaml для снижения ложных срабатываний AST.
- OpenRouter prompt‑парсер теперь корректно обрабатывает пустые списки.

### Обновление 2026-01-04 (v0.15.22)
- Добавлены публичные страницы "Инструкции" (видео/текст) и "Примеры генераций".
- Админ-панель: управление инструкциями и библиотекой примеров, включая статистику запусков.
- Примеры генераций: кнопка запуска переносит в редактирование и подставляет промпт.
- Примерка: на шаге загрузки одежды добавлены рекомендации к фото (без лица/рук/ног).
- В шапках всех страниц добавлена ссылка на Telegram-канал и пункт меню "Написать в MAX".

### Обновление 2025-12-15 (v0.15.20)
- Реферальный код теперь генерируется единообразно (user_id + SECRET_KEY) во всех auth/referral эндпоинтах — приглашённые и зарегистрированные пользователи вновь учитываются в «Всего приглашённых».
- Кнопка Google Sign-In получает SDK через общий загрузчик с повтором при сбое, поэтому появляется с первой загрузки страницы.
- Карточки реферальной статистики стали адаптивными: на мобильных три метрики раскладываются по рядам и не ломают сетку.

### Обновление 2025-12-14 (v0.15.19)
- Админка: начисление кредитов заменено на редактирование баланса (PUT `/api/v1/admin/users/{id}/credits`, кнопка ✏️ в таблице пользователей, модал с новым балансом и опциональным комментарием).

### Обновление 2025-12-10 (v0.15.11)
- Исправлена ошибка `cannot access local variable 'base_image_url'` в Celery-задаче редактирования: локальное сохранение/восстановление базового изображения не ломает первую генерацию.
- Исправлен `NameError: urlparse is not defined` при сборке публичных ссылок (to_public_url) в задачах редактирования.

### Обновление 2025-12-10 (v0.15.10)
- Редактирование: если базовое изображение пропало локально, Celery перекачивает его по публичному URL, нормализует и синхронизирует `base_image_url` в чат-сессии — повторные генерации не падают.
- API: `/fitting/result/{task_id}` теперь возвращает сохранённое `error_message`, чтобы фронт показывал реальную причину сбоя.
- UI примерки/редактирования: дисклеймер приведён к единому тексту "Результат генерируется внешним AI-сервисом и может отличаться от ваших ожиданий!".

### Обновление 2025-12-09 (v0.15.9)
- Виртуальная примерка: user-фото теперь принудительно выпрямляется и сохраняется без EXIF, чтобы не случались повороты у провайдеров/на фронте.
- Item-фото перед отправкой подгоняется под соотношение сторон user-фото без искажений (масштаб + паддинг на прозрачном фоне) и работает для обоих операторов (kie.ai/OpenRouter).
- Кнопки скачивания примерки/редактирования работают с относительными URL и внешним backend (фолбэк через fetch→blob или открытие в новой вкладке).
- В UI добавлен дисклеймер: результат генерируют внешние AI-сервисы, возможны расхождения с ожиданиями.

### Обновление 2025-12-05 (v0.15.8)
- Брендинг: новый логотип `/logo.png` во всех публичных и внутренних layout-компонентах.
- Контакты: страница `/contacts` приведена к стилю целевого лендинга (единый хедер/футер, карточки реквизитов).

### Обновление 2025-12-05 (v0.15.7)
- Админка: выбор основного/запасного провайдера генерации (kie.ai / OpenRouter) без перезапуска; отключение fallback.
- kie.ai: таймаут 180с, расширенный polling; фронт предупреждает после 60с, что генерация ещё идёт.
- Профиль: убран freemium UI, показываем только актуальные данные биллинга v5 (кредиты, подписка, остаток действий).
- Referral: бонус рефереру начисляется после первой успешной покупки приглашённого пользователя.

### Обновление 2025-12-05 (v0.15.6)
- Публичный лендинг `/` перерисован под новый макет (две карточки функций, обновлён блок AI-ассистента, CTA по тарифам/безопасности).
- Брендинг: новый логотип в публичных шапках/футерах и внутренних layout-компонентах.
- Экран входа: единый стиль форм, кнопки Google/VK одинаковой высоты/ширины на мобиле и десктопе; CTA и поля приведены к общему UI.

### ✅ Backend (FastAPI)

- **Аутентификация**:
  - Email/Password регистрация и вход
  - Google OAuth (опционально)
  - Telegram WebApp (legacy, обратная совместимость)
  - JWT токены (60 мин)
  - bcrypt password hashing (12 rounds)

- **API Endpoints**:
  - `/api/v1/auth-web/*` - Web authentication
  - `/api/v1/generations/*` - Image generation
  - `/api/v1/payments/*` - Payment system
  - `/api/v1/referrals/*` - Referral program
  - `/api/v1/admin/*` - Admin panel
  - `/api/v1/content/*` - Публичные инструкции и примеры
  - `/api/v1/admin/instructions` и `/api/v1/admin/examples` - управление контентом
  - `/api/v1/content/example-tags` - метки примеров для фильтрации

- **Database** (PostgreSQL):
  - Users (email, telegram_id, auth_provider)
  - Generations (history)
  - Payments (YuKassa)
  - Referrals
  - Chat History

- **External Services**:
  - OpenRouter (Gemini image preview + GPT-4.1 Mini) - image generation & prompts
  - YuKassa - payments

### ✅ Frontend (React + TypeScript)

- **Pages**:
  - LoginPage - Email/Password вход
  - RegisterPage - регистрация
  - HomePage - выбор функции
  - FittingPage - виртуальная примерка
  - EditingPage - AI редактирование
  - ProfilePage - профиль пользователя
  - AdminPage - админ-панель
  - InstructionsPage - инструкции по использованию
  - ExamplesPage - библиотека примеров генераций

- **State Management** (Zustand):
  - authStore - аутентификация
  - Persistent state в localStorage

- **Auth Features**:
  - Email/Password формы
  - Google Sign-In кнопка
  - Auto-login для Telegram
  - JWT token management
  - Protected routes

### ✅ Testing

- E2E тестирование с Playwright MCP
- Регистрация протестирована ✅
- Вход протестирован ✅
- JWT токены работают ✅
- **Виртуальная примерка протестирована E2E** ✅
  - Все 3 шага успешно выполняются
  - Загрузка фото работает корректно
  - API запросы доходят до backend
  - Celery задачи создаются

---

## 🐛 Исправленные проблемы

### v0.12.3 (2025-01-20) - Auth Testing & Fixes ✅

**Тестирование с Playwright MCP**

| # | Проблема | Решение | Файл |
|---|----------|---------|------|
| 1 | Google Identity Services загружался с ошибками | Добавлен retry механизм (10 × 500ms) | GoogleSignInButton.tsx:34-105 |
| 2 | Ошибки в консоли "Google not loaded" | Улучшена обработка асинхронной загрузки | GoogleSignInButton.tsx:43-80 |
| 3 | Неверная проверка загрузки скрипта | Добавлен `window.google?.accounts?.id` check | GoogleSignInButton.tsx:44, 135 |

**Результаты E2E тестирования**:
- ✅ Регистрация: создан тест пользователь `test@example.com`
- ✅ Вход: email авторизация работает корректно
- ✅ Редирект: автоматический переход на главную после входа
- ✅ Все страницы: главная, профиль, примерка - загружаются
- ✅ Google Sign-In: кнопка отображается без ошибок
- ⚠️ Google Client ID: требуется валидный для продакшена

**Скриншоты тестирования**: `.playwright-mcp/` (8 скриншотов)

**Итог**: Авторизация полностью функциональна. Нет критических ошибок.

---

### v0.12.2 (2025-11-18)

| # | Проблема | Решение | Файл |
|---|----------|---------|------|
| 1 | kie.ai не получал фотографии для try-on | Перешли на OpenRouter, передаём обе фото в base64 | fitting.py |
| 2 | Файлы недоступны по HTTP | Добавлен static file serving `/uploads` | main.py:107 |
| 3 | Отсутствует BACKEND_URL | Добавлена настройка (default: localhost:8000) | config.py:30 |
| 4 | Database constraint violation | Добавлен placeholder для `prompt` поля | fitting.py:149 |
| 5 | Proxy блокирует localhost | Использовать `NO_PROXY=localhost,127.0.0.1` | - |
| 6 | Redis/Celery не запущены | Требуется запуск перед тестированием | - |

**Итог**: Виртуальная примерка теперь функционирует полностью. E2E тестирование подтвердило работоспособность всех компонентов.

**Требования для запуска**:
```bash
redis-server --daemonize yes
celery -A app.tasks.celery_app worker --loglevel=info -Q fitting,editing,maintenance
```

### v0.12.1 (2025-11-18)

| # | Проблема | Решение | Файл |
|---|----------|---------|------|
| 1 | Новые пользователи получают 0 кредитов | `balance_credits=10` при регистрации | auth_web.py:143, 334 |
| 2 | Кнопка "Пропустить" устанавливает zone=null | Теперь устанавливает `zone='body'` | Step3Zone.tsx:50 |
| 3 | Неточный текст подсказки | Обновлен текст (убрано "AI определит автоматически") | Step3Zone.tsx:160-162 |

**Итог**: Новые пользователи получают 10 кредитов + 10 Freemium действий = 15 генераций в первый месяц

### v0.12.0 (2025-11-18)

| # | Проблема | Решение | Файл |
|---|----------|---------|------|
| 1 | Missing email-validator | `pip install email-validator` | requirements.txt |
| 2 | Pydantic forward reference | `from __future__ import annotations` | schemas/auth_web.py |
| 3 | "login is not a function" | Исправлены методы в useAuth | hooks/useAuth.ts |
| 4 | Missing auth routes | Добавлены роуты | App.tsx |
| 5 | API 404 errors | Обновлены endpoints | api/authWeb.ts |
| 6 | Router prefix mismatch | `/auth` → `/auth-web` | endpoints/auth_web.py |
| 7 | AuthProvider enum | lowercase → uppercase | models/user.py + DB |
| 8 | Cached statement error | Backend restart | - |

**Подробнее**: [docs/WEB_AUTH_IMPLEMENTATION.md](WEB_AUTH_IMPLEMENTATION.md)

---

## 📦 Deployment Status

### Development

```bash
# Статус серверов
✅ PostgreSQL: Running (Docker, port 5432)
✅ Backend: Running (uvicorn, port 8000)
✅ Frontend: Running (Vite, port 5173)
```

### Production

- **Not deployed yet**
- Готов к деплою на VPS
- См. [docs/deployment/DEPLOY.md](deployment/DEPLOY.md)

---

## 🔑 Ключевые файлы для понимания системы

### Backend
```
backend/
├── app/
│   ├── api/v1/endpoints/auth_web.py    # Web auth endpoints
│   ├── models/user.py                  # User model + AuthProvider enum
│   ├── schemas/auth_web.py             # Pydantic schemas
│   ├── core/security.py                # Password hashing, JWT
│   └── main.py                         # FastAPI app
└── .env                                # Конфигурация
```

### Frontend
```
frontend/
├── src/
│   ├── pages/
│   │   ├── LoginPage.tsx               # Страница входа
│   │   └── RegisterPage.tsx            # Страница регистрации
│   ├── store/authStore.ts              # Zustand auth state
│   ├── api/authWeb.ts                  # API client
│   ├── hooks/useAuth.ts                # Auth hook
│   └── App.tsx                         # Routes
└── .env                                # API URL, Google Client ID
```

### Database
```sql
-- Таблица users поддерживает 3 типа аутентификации:
- email + password_hash           (Web users)
- oauth_provider_id + auth_provider=GOOGLE  (Google OAuth)
- telegram_id + auth_provider=TELEGRAM      (Legacy Telegram)
```

---

## 🎯 Следующие шаги

### Готово к реализации
- [ ] Email verification (отправка письма с подтверждением)
- [ ] Password reset flow
- [ ] Two-factor authentication (2FA)
- [ ] Social login (Facebook, Apple)
- [ ] User profile editing

### Требует настройки
- [ ] Google OAuth credentials (опционально)
- [ ] ЮKassa production credentials
- [ ] Production domain и SSL
- [ ] Email service (SendGrid, Mailgun)

### Оптимизация
- [ ] Redis session storage (вместо JWT в localStorage)
- [ ] Rate limiting per endpoint
- [ ] Monitoring (Sentry, DataDog)
- [ ] CDN для статики

---

## 📚 Документация

### Для разработчиков
- [WEB_AUTH_IMPLEMENTATION.md](WEB_AUTH_IMPLEMENTATION.md) - детальная документация веб-аутентификации
- [CHANGELOG.md](../CHANGELOG.md) - история изменений
- [QUICK_START.md](../QUICK_START.md) - быстрый старт
- [ENV_SETUP_GUIDE.md](../ENV_SETUP_GUIDE.md) - настройка окружения

### Для GPT Codex / Claude

**Важный контекст**:

1. **Проект преобразован** из Telegram WebApp в полноценное веб-приложение
2. **Три способа авторизации**: Email/Password, Google OAuth, Telegram (legacy)
3. **AuthProvider enum**: используйте UPPERCASE значения (`EMAIL`, `GOOGLE`, `TELEGRAM`)
4. **Router prefix**: `/api/v1/auth-web/` для web auth (НЕ `/api/v1/auth/`)
5. **Dev режим**: auto-login отключен, используйте `/login` для входа
6. **После изменения DB schema**: всегда перезапускайте backend

**Перед началом работы**:
- Прочитайте [WEB_AUTH_IMPLEMENTATION.md](WEB_AUTH_IMPLEMENTATION.md)
- Проверьте [CHANGELOG.md](../CHANGELOG.md) для понимания последних изменений
- Убедитесь что PostgreSQL и оба сервера запущены

---

## 🧪 Как протестировать

### Быстрый тест

```bash
# 1. Запустите все сервисы
./start-dev.sh

# 2. Откройте браузер
open http://localhost:5173/register

# 3. Зарегистрируйтесь
Email: test@example.com
Password: Test123!@#
First Name: Test
Last Name: User

# 4. Проверьте вход
Перейдите на /login и войдите с теми же credentials
```

### E2E тест с Playwright

```javascript
// Попросите Claude Code:
"Открой http://localhost:5173/register через Playwright
и протестируй регистрацию нового пользователя"
```

---

## 💡 Tips для AI-ассистентов

### При добавлении новых features:

1. **Всегда проверяйте актуальную документацию**:
   - `docs/WEB_AUTH_IMPLEMENTATION.md`
   - `CHANGELOG.md`
   - `docs/PROJECT_STATUS.md` (этот файл)

2. **Enum values всегда uppercase**:
   ```python
   # ✅ Правильно
   AuthProvider.EMAIL

   # ❌ Неправильно
   AuthProvider.email
   ```

3. **После изменения database schema**:
   ```bash
   # ОБЯЗАТЕЛЬНО перезапустите backend
   kill <backend-pid>
   cd backend && uvicorn app.main:app --reload
   ```

4. **API endpoints naming**:
   ```typescript
   // ✅ Правильно
   '/api/v1/auth-web/register'

   // ❌ Неправильно
   '/auth/register'
   '/api/v1/auth/register'
   ```

5. **Pydantic schemas с forward references**:
   ```python
   from __future__ import annotations

   # Класс должен быть определен ПЕРЕД использованием
   class UserProfile(BaseModel): ...

   class LoginResponse(BaseModel):
       user: UserProfile  # Теперь это работает
   ```

---

## 📞 Контакты и поддержка

- **Разработчик**: Claude Code + GPT Codex
- **Репозиторий**: Local project
- **Версия**: v0.12.0
- **Дата релиза**: 2025-11-18

---

## ✅ Production Readiness Checklist

### Backend
- [x] Email/Password authentication
- [x] JWT tokens
- [x] Password hashing (bcrypt)
- [x] Database migrations
- [x] API documentation (Swagger)
- [ ] Email verification
- [ ] Password reset
- [ ] Rate limiting per endpoint
- [ ] Monitoring (Sentry)

### Frontend
- [x] Login page
- [x] Registration page
- [x] JWT token management
- [x] Protected routes
- [x] Error handling
- [ ] Email verification UI
- [ ] Password reset UI
- [ ] Loading states
- [ ] Accessibility (a11y)

### DevOps
- [x] Docker setup
- [x] Development environment
- [ ] Production deployment
- [ ] SSL certificates
- [ ] CI/CD pipeline
- [ ] Automated backups
- [ ] Monitoring dashboard

---

**Status**: ✅ Ready for production deployment after email verification implementation

**Next Release**: v0.13.0 (Email Verification)
