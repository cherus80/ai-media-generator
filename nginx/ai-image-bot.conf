# Nginx конфигурация для AI Generator (Production на Beget VPS)
# Разместить в /etc/nginx/sites-available/ai-image-bot.conf
# Создать симлинк: ln -s /etc/nginx/sites-available/ai-image-bot.conf /etc/nginx/sites-enabled/

# Upstream для backend API
upstream backend_api {
    server localhost:8000;
    keepalive 32;
}

# Upstream для frontend
upstream frontend_app {
    server localhost:3000;
    keepalive 32;
}

# Базовый rate limiting и ограничения соединений
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=chat_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=generate_limit:10m rate=5r/s;
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

# HTTP -> HTTPS редирект
server {
    listen 80;
    listen [::]:80;
    server_name ai-bot-media.mix4.ru www.ai-bot-media.mix4.ru;

    # ACME challenge для Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Редирект на HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS сервер (основной)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name ai-bot-media.mix4.ru www.ai-bot-media.mix4.ru;

    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/ai-bot-media.mix4.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ai-bot-media.mix4.ru/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/ai-bot-media.mix4.ru/chain.pem;

    # SSL настройки (Mozilla Intermediate конфигурация)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # OCSP Stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # VK OAuth support - allow popups for OAuth flow
    add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;

    # Content Security Policy - разрешения для Google OAuth, VK ID SDK, Yandex и Telegram Login Widget
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://unpkg.com https://*.vk.com https://*.vk.ru https://accounts.google.com https://apis.google.com https://telegram.org https://*.telegram.org; style-src 'self' 'unsafe-inline' https://unpkg.com https://fonts.googleapis.com https://accounts.google.com; connect-src 'self' https://*.vk.com https://*.vk.ru https://id.vk.com wss://*.vk.com https://accounts.google.com https://*.googleapis.com https://telegram.org https://*.telegram.org; frame-src 'self' https://*.vk.com https://*.vk.ru https://id.vk.com https://accounts.google.com https://telegram.org https://*.telegram.org https://oauth.telegram.org; img-src 'self' data: https: blob: https://telegram.org https://*.telegram.org; font-src 'self' data: https://fonts.gstatic.com; object-src 'none'; base-uri 'self'; form-action 'self';" always;

    # Логи
    access_log /var/log/nginx/ai-image-bot-access.log;
    error_log /var/log/nginx/ai-image-bot-error.log warn;

    # Ограничение соединений и частоты запросов
    limit_conn conn_limit 50;
    limit_conn_status 429;
    limit_req_status 429;

    # Максимальный размер загружаемых файлов (до 200MB для видео и изображений)
    client_max_body_size 200M;
    client_body_buffer_size 128k;

    # Таймауты
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json image/svg+xml;
    gzip_disable "MSIE [1-6]\.";
    gzip_comp_level 6;

    # Auth endpoints (усиленный лимит)
    location /api/v1/auth-web/ {
        limit_req zone=auth_limit burst=15 nodelay;

        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать API запросы
        proxy_cache_bypass $http_upgrade;
        add_header X-Cache-Status $upstream_cache_status;
    }

    # AI assistant чат
    location /api/v1/editing/chat {
        limit_req zone=chat_limit burst=20 nodelay;

        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать API запросы
        proxy_cache_bypass $http_upgrade;
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Генерации (примерка/редактирование)
    location /api/v1/editing/generate {
        limit_req zone=generate_limit burst=10 nodelay;

        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать API запросы
        proxy_cache_bypass $http_upgrade;
        add_header X-Cache-Status $upstream_cache_status;
    }

    location /api/v1/fitting/generate {
        limit_req zone=generate_limit burst=10 nodelay;

        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать API запросы
        proxy_cache_bypass $http_upgrade;
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Backend API (FastAPI)
    location /api/ {
        limit_req zone=api_limit burst=60 nodelay;

        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать API запросы
        proxy_cache_bypass $http_upgrade;
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Swagger UI (закомментировано для production безопасности)
    # location /docs {
    #     proxy_pass http://backend_api;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # location /openapi.json {
    #     proxy_pass http://backend_api;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    # }

    # WebSocket для real-time updates (если будет использоваться)
    location /ws/ {
        proxy_pass http://backend_api;
        proxy_http_version 1.1;

        # WebSocket headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket таймауты
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # Статические файлы (загруженные изображения)
    location /uploads/ {
        alias /var/www/ai-image-bot/uploads/;

        # Cache
        expires 24h;
        add_header Cache-Control "public, must-revalidate";

        # Security: запретить выполнение скриптов
        location ~ \.(php|jsp|asp|sh|cgi)$ {
            deny all;
        }
    }

    # Frontend (React SPA)
    location / {
        proxy_pass http://frontend_app;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Keepalive
        proxy_set_header Connection "";

        # Не кешировать HTML
        proxy_cache_bypass $http_upgrade;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# Альтернативная конфигурация для static frontend (вместо proxy_pass к Node.js)
# Раскомментировать, если используется production build frontend (npm run build)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name your-domain.com www.your-domain.com;
#
#     # SSL конфигурация (та же что выше)
#
#     root /var/www/ai-image-bot/frontend/dist;
#     index index.html;
#
#     # Frontend static files
#     location / {
#         try_files $uri $uri/ /index.html;
#
#         # Cache HTML (не кешируем)
#         location ~ \.html$ {
#             add_header Cache-Control "no-cache, must-revalidate";
#         }
#
#         # Cache JS/CSS/Images
#         location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#             expires 1y;
#             add_header Cache-Control "public, immutable";
#         }
#     }
#
#     # Backend API (то же что выше)
#     location /api/ {
#         proxy_pass http://backend_api;
#         # ... (остальная конфигурация)
#     }
# }
